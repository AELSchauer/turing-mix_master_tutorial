<%= form_for [@artist, @song] do |f| %>
  <% if @song.errors.any? %>
    <div class="error_notification">
      <h2>
        <%= pluralize(@song.errors.count, "error") %> prohibited this record from being saved:
      </h2>
      <ul>
        <% @song.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <br />
  <% end %>
  <table class="form_table">
    <tr>
      <th>
        <%= f.label :title %>
      </th>
      <td>
        <%= f.text_field :title %>
      </td>
    </tr>
  </table>
  <br />
  <%= f.submit "Submit", class: 'button' %>
<% end %>